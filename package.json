{
  "name": "root",
  "version": "0.0.0",
  "description": "workspace root",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b ./apps/app-a/tsconfig.json ./apps/app-b/tsconfig.json"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "typescript": "^5.6.2"
  }
}
